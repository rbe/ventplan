package com.westaflex.wac

/*
abluftList = new ObservableList();
// Akustik - Zuluft
//bind(source: model.map.anlage.hygiene, sourceProperty: "nachricht",     target: anlageHygieneNachricht,     targetProperty: "text")
akustikAbluftRaumbezeichnung.items = model.map.raum.raume.each { raum ->
    abluftList.add(raum.raumBezeichnung)
}

zuluftList = new ObservableList();
// Akustik - Abluft
akustikZuluftRaumbezeichnung.items = model.map.raum.raume.each { raum -> 
    zuluftList.add(raum.raumBezeichnung)
}
*/